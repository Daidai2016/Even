diff --git a/AI_Skills/Graphic_Design/references/Prompt_Framework.md b/AI_Skills/Graphic_Design/references/Prompt_Framework.md
index e49a042..c02b14b 100644
--- a/AI_Skills/Graphic_Design/references/Prompt_Framework.md
+++ b/AI_Skills/Graphic_Design/references/Prompt_Framework.md
@@ -1,6 +1,6 @@
 # 视觉任务与生图提示编写指南
 
-版本：2.0 / 2026-09-10
+版本：2.1 / 2026-09-10
 依据：[OpenAI Image prompting](https://developers.openai.com/api/docs/guides/image-prompting)，2026-09-10 核对。
 
 ## 1. 定位与使用
@@ -14,6 +14,14 @@
 - 非生图设计任务直接说明目标、准确内容、参考用途、约束和交付标准；不强制写成生图 Prompt。
 - 动态任务在已确认静态设计上追加时长、帧率、动作、节奏、循环与镜头许可，沿用动态路由；静态图像 API 设置不适用于视频。
 
+### 1.1 RCE 上层任务契约与实际 Prompt
+
+保留 RCE（Role–Constraints–Examples，角色—约束—示例）作为上层任务契约：R 只记录影响本次判断的职责，C 记录目标、硬锁、允许变化与交付验收，E 记录参考来源、用途和边界。简单任务可在内部完成这些判断，不要求展示角色、契约或完整分析；复杂任务需要交接时再明确列出。
+
+实际提交给 GPT Image 的 Prompt 描述本轮画面，不照搬 RCE 全文、履历、仓库操作或审核流程。工具参数和后期制作清单分开记录。H1/H2/H3 应转成位置、比例和阅读关系；LOCKED 应转成具体保留项，不能因简化提示而遗漏。
+
+OpenAI 官方 Image Prompting 指南是 GPT Image 实际提示词写法的最高专业依据；RCE 负责本次任务边界与验收，两者职责不同。第 4 节四类模板继续用于实际 Prompt，不作为 RCE 固定格式。
+
 ## 2. 编写原则
 
 1. **先写结果。** 说明做什么图、给谁看、用在哪里，写清主体、画幅、构图和关键位置；不要求先输出设计师身份或完整分析。
@@ -22,11 +30,28 @@
 4. **准确文字单独标明。** 用引号包住原文，说明位置、排印和出现次数；必要时逐字澄清特殊拼写。说明不添加额外文字，生成后逐字核对可读性、数字及标点。
 5. **编辑分清改与不改。** 指定区域、对象和变化，明确身份、形状、构图、配色、光线、标签等保留项。禁止项只针对本轮实际风险，不把通用负面词库整段附加到每个提示。
 6. **参考逐张分工。** 用编号或文件名区分底图、主体、构图、风格和待合成资产，说明只采用哪些属性以及组合关系。只有参考用途的图不能替代唯一底图。
-7. **逐轮收敛。** 使用已确认输出作为下一轮输入，每轮修改一个问题或一组相关问题，重申关键保留项并检查结果。不要靠不断追加重复措辞代替失败诊断。
+7. **逐轮收敛。** 使用已确认输出作为下一轮输入，默认每轮只改一个明确变量；确需联动时记录整组范围与原因，重申关键保留项并检查结果。不要靠不断追加重复措辞代替失败诊断。
 8. **格式服务维护。** 自然段、分段指令、标签或结构化字段均可；不宣称特殊语法更有效，不强制英文或长提示。默认使用中文，保留准确专名和用户指定语言。
 
 以上是对官方原则的工作台应用。完整后期步骤、保存路径、审核记录不必塞入图像提示；需要交付时单独提供。
 
+### 2.1 六段组织顺序
+
+**Result → Subject → Composition → Visible Details → Style/Medium → Constraints**
+
+这是工作台根据官方原则整理的助记顺序，并非官方命名框架或强制语法；按任务删减，不必逐段输出。
+
+| 部分 | 需要表达的画面要求 |
+| --- | --- |
+| Result｜结果 | 图像类型、用途、画幅与交付阶段 |
+| Subject｜主体 | 对象、数量、身份、动作及入画范围 |
+| Composition｜构图 | 位置、比例、视角、裁切、空间关系、留白与文字安全区 |
+| Visible Details｜可见细节 | 形态、纹理、材质、颜色与光线 |
+| Style/Medium｜风格与媒介 | 摄影、插画、纸张丝印、三维等表现方式 |
+| Constraints｜约束 | 准确文案、保留项、修改边界、禁止项与目标输出 |
+
+第 4 节已有新建、参考、编辑和合成模板按此思路裁剪，无需增加第二套模板。
+
 ## 3. 约束、文字与设置
 
 - 沿用[参数硬锁](shared/parameter-lock.md)：用户指定的底图、主体、配色、文字、Logo、尺寸及输出要求不得被默认值覆盖。冲突按[冲突处理](shared/conflict-resolution.md)解决。
@@ -34,7 +59,7 @@
 - 用户要求图内文字时允许直接生成并逐字验收。需要标准 Logo、精确字体、可编辑排字或生产级一致性时使用官方资产及可控后期；错误文字不可作为合格结果。详细排印见[字体规则](shared/typography.md)。
 - 提示描述画面和目标画幅；`model`、`size`、`quality`、`background`、格式等实际 API 设置单列，仅在工具支持时使用。网页、Codex 工具与 API 不共用一套参数。
 - 请求透明背景不等于文件具有 alpha；写下尺寸不等于输出达到该尺寸。打开实际文件验证，不能把放大或改 PPI 当作原生细节通过。
-- 生成编辑不能保证区域像素完全不变；有此要求时，在原图上可控合成已批准修改，并对保护区作差异核验。
+- 生成编辑不能保证区域像素完全不变；要求 pixel-identical 时，必须在原图上确定性合成已批准修改，再按[生图生产路由第 3.1 节](routes/production/image-generation.md)对保护区作逐像素核验。
 
 ## 4. 四类可裁剪模板
 
diff --git a/AI_Skills/Graphic_Design/references/routes/production/image-generation.md b/AI_Skills/Graphic_Design/references/routes/production/image-generation.md
index e3db251..23b9451 100644
--- a/AI_Skills/Graphic_Design/references/routes/production/image-generation.md
+++ b/AI_Skills/Graphic_Design/references/routes/production/image-generation.md
@@ -21,7 +21,7 @@
 - 每张输入图登记：编号/文件名、底图或参考角色、允许借鉴、必须保留、不可复制。未看见文件时不凭文件名猜图。
 - 从实际图中读取主体数量、位置、方向、比例、原色、关键结构和文字区；用户指定内容设为 `LOCKED`。
 - 记录目标画幅与像素；印刷另记物理尺寸、分辨率、色彩环境和出血。比例与像素矛盾时先明确哪个是准值。
-- 编辑任务先列 `KEEP / CHANGE / LOCKED`；局部修改写明区域、对象及边界。极严格的像素保留宜在原图上用可控蒙版/图层合成验收，不能承诺生成过程绝对不影响选区外。
+- 编辑任务先列 `KEEP / CHANGE / LOCKED`；局部修改写明区域、对象及边界。要求 pixel-identical 时必须按第 3.1 节确定性合成并验收，不能承诺生成过程绝对不影响选区外。
 - 缺省值只补用户未指定项。保色、白底、无字、透明背景、镜头或风格均不能擅自替换用户明确要求。
 
 ## 3. 建立可执行提示
@@ -34,13 +34,25 @@
 
 需要图内文字时，用引号提供准确文案并明确位置、排印和出现次数，允许按用户需求直接生成，逐字复核。需要标准 Logo、精确字体、可编辑排字或严格产品标签时，保留官方资产并采用可控后期；不把生成候选当作已核验品牌终稿。
 
+### 3.1 pixel-identical 与确定性合成
+
+官方明确指出：区域必须 pixel-identical 时，应把确认后的编辑合成回原图，不能仅依靠提示词。工作台据此执行以下生产与验收要求：
+
+1. 保存原图与保护区域定义，先区分“视觉一致”与“逐像素相同”。
+2. 生成候选只提供允许修改的内容；在原图上用图层和明确蒙版合成。保护区域直接使用原图像素；羽化边缘必须位于允许修改范围内。
+3. 若承诺逐像素相同，保护区不得经历缩放、重采样、整体调色或有损重编码。保留相同像素网格、通道、位深和色彩解释；使用无损验收文件。
+4. 重开导出文件，对保护区域逐通道比较像素值（含 alpha）。差异像素数与最大通道差均为 0，才记录该区域 pixel-identical 通过；视觉预览、OCR 或文件大小相同不能替代此项检查。
+5. 另需 JPEG、改尺寸或转换色彩时，单独验收其用途，不沿用无损母版的像素一致结论。
+
+这些步骤是工作台的后期验收约定；生成工具返回成功不代表已完成确定性合成。
+
 ## 4. 生成、选择与返修
 
 1. 上传或展示本轮真正使用的输入图；本地图片先实际打开检查。
 2. 按用户要求的数量生成；未指定时先做一个明确方向，避免无目的批量消耗。
 3. 对照输入与硬锁检查候选，而不只凭“好看”选择；标出问题属于主体、构图、字体、颜色、肌理还是生产规格。
-4. 沿用已确认版本，每次修改一个明确问题或同一组相关问题；重申保留项，禁止悄悄换底图。
-5. 修改保护区域附近内容后，对照原图检查边缘、位置、文字安全区和品牌资产；有像素保留要求时在相同尺寸、色彩条件下核对区域差异。
+4. 将上一轮已确认输出作为下一轮输入，默认每轮只改一个明确变量；确需联动时记录整组范围与原因。重申保留项，先比较结果再补充指令，禁止悄悄换底图。
+5. 记录输入/输出版本、实际 Prompt、参数、本轮变量和通过/失败原因；对照原图检查边缘、位置、文字安全区和品牌资产。pixel-identical 按第 3.1 节验收。
 6. 发生持续主体漂移或局部无法受控时，回到已确认母版，缩小编辑范围或转入确定性的后期制作；不通过反复整图重生成掩盖失败。
 
 ## 5. 验收与 Adobe 交接
diff --git a/docs/AIGC_CREATIVE_RULES.md b/docs/AIGC_CREATIVE_RULES.md
index f695634..e2c9cbf 100644
--- a/docs/AIGC_CREATIVE_RULES.md
+++ b/docs/AIGC_CREATIVE_RULES.md
@@ -69,6 +69,16 @@
 5. 使用新内容、新尺寸或新风格复测，确认能力可迁移。
 6. 只有结果稳定、边界清晰且可复现时，才沉淀为正式工作流或 skill。
 
+### 3.1 图像提示词的训练与沉淀
+
+RCE 上层任务契约边界与 GPT Image 实际提示词写法继续维护在 `AI_Skills/Graphic_Design/references/Prompt_Framework.md`；执行和文件验收维护在 `AI_Skills/Graphic_Design/references/routes/production/image-generation.md`，以原文件登记的 OpenAI 官方指南为专业依据。本文件只补训练证据要求，不另建提示词规范或把六段组织方式称为已验证 Skill。
+
+在现有实验记录中保留：任务类型（新建/编辑/参考/合成）、原始契约、实际 Prompt、输入图角色与版本、实际工具/模型及可用参数、每轮变更变量、候选结果、保护项检查和人工结论。未知或不可获取的参数标为未提供，不补造种子或模型信息。
+
+比较提示词时固定可固定的输入、参数与评价尺度；每轮优先只改变一个变量。记录失败样本与漂移，不用单次好图证明稳定复现。pixel-identical 任务须另记确定性合成文件、保护区域和导出差异检查，不能用生成成功替代后期验收。
+
+按官方文档补充写法属于规范维护；新增创意方法的能力晋级仍需现有第 8 节的实验与人工门禁。是否更换模型不在本次提示词修订中默认决定。
+
 ## 4. Skill 结构
 
 一个 skill 只解决一类稳定问题。不将审美、版式、字体、形式创意和工具操作无边界地合并在一个 skill 中。
diff --git a/docs/VISUAL_WORKBENCH.md b/docs/VISUAL_WORKBENCH.md
index f1b1dd9..96ea5ec 100644
--- a/docs/VISUAL_WORKBENCH.md
+++ b/docs/VISUAL_WORKBENCH.md
@@ -49,7 +49,7 @@
 
 ## Project 总指令
 
-版本：2026-09-10 / 2.0。本节是云端 Project 指令的本地维护源；云端为同步副本。已有八个主题聊天共同继承，不再逐个复制总规则。以下文本用于项目的“指令”字段。
+版本：2026-09-10 / 2.1。本节是云端 Project 指令的本地维护源；云端为同步副本。已有八个主题聊天共同继承，不再逐个复制总规则。以下文本用于项目的“指令”字段。
 
 ```text
 你是「AI视觉设计工作台」的设计与生产协作者。目标是把设计需求推进到可检查、可继续编辑、可复用的成果。默认用中文，结论先行、表达精炼；Adobe 菜单采用中文名称，专有名词、文件名和 Skill 英文 ID 保持不变。
@@ -59,11 +59,11 @@
 本地维护源为当前实际打开的 Codex Design 仓库；本机已核实为 D:\Codex_Design，历史 D:\Codex\_Design 是错误路径。云端的本地路径文字不赋予文件访问或同步能力。只使用实际可读的项目来源、附件、连接和工具；未读取、未调用或未验证的动作必须如实说明。
 
 二、设计与硬锁
-直接明确目标、受众、媒介、约束和各参考图用途；简单任务直接给可用提示，不强制角色设定、契约或完整分析。将用户指定的底图、主体数量/身份/位置、原色、构图、画幅、材质、准确文案、Logo、字体、输出和动态参数记为 LOCKED。执行顺序按用户明确要求、品牌/项目硬约束、Skill 专属规则、命中路由、共享规则、默认值处理；冲突必须指出，不能静默解锁。
+保留 RCE 作为上层任务契约，明确目标、受众、媒介、约束和各参考图用途；简单任务直接给可用提示，不强制展示角色、契约或完整分析。将用户指定的底图、主体数量/身份/位置、原色、构图、画幅、材质、准确文案、Logo、字体、输出和动态参数记为 LOCKED。执行顺序按用户明确要求、品牌/项目硬约束、Skill 专属规则、命中路由、共享规则、默认值处理；冲突必须指出，不能静默解锁。
 底图、构图参考、风格参考、待合成资产分别标明；仅作参考的图不能替代唯一底图。第一视觉由传播目标决定，可为标题、主体或品牌。先安排层级、网格、裁切和文字安全区，再处理色彩、材质和细节。中文标题、正文、西文、数字与标点分别核对；字体参数与授权未知时标明未核验。
 
 三、ChatGPT Images 生图与修图
-明确是新建、基于参考创作，还是编辑已确认图像；先读取实际输入图。按目标画面、用途、主体、构图、可见细节、参考职责和必要约束组织简洁提示；复杂任务按需分段，四类可裁剪模板沿用 Prompt_Framework.md。准确文字用引号标明原文，并说明位置、排印和出现次数；工具参数与提示正文分开，仅使用当前接口实际支持的设置。
+明确是新建、基于参考创作，还是编辑已确认图像；先读取实际输入图。按目标画面、用途、主体、构图、可见细节、参考职责和必要约束组织简洁提示；复杂任务按需分段，六段组织顺序及四类可裁剪模板沿用 Prompt_Framework.md。六段顺序是工作台助记，实际 Prompt 与 RCE 分开，不是官方强制语法。准确文字用引号标明原文，并说明位置、排印和出现次数；工具参数与提示正文分开，仅使用当前接口实际支持的设置。
 用户指定 ChatGPT Images 时使用当前可用的对应生图能力；工具不可用应说明，不静默切换模型、收费 API 或生成方式。保留每张图的角色，每轮沿用已确认版本，只改变明确指定的变量，并复核保护区域。白底、无文字和换色均不能覆盖用户要求。
 生成后实际检查主体、构图、边缘、文字、Logo、颜色、纹理、像素尺寸和透明度。生成结果不自动等于可编辑母版或印刷终稿；图内文字可按需求生成并逐字验收；标准 Logo、精确字体、可编辑排字及生产级一致性采用官方资产与可控后期。尺寸、局部保真或文字不达标时返回修正，不用放大、改分辨率属性或“生成成功”冒充质量通过。详细流程沿用 image-generation.md。
 
@@ -125,6 +125,12 @@
 
 任务进一步涉及某个未上传路由、品牌手册或源 Skill 时，再上传对应原文件与必要依赖；不要全量复制插件缓存、日志或客户资料。上传参考文件不等于安装 Skill，也不授予本机操作权限。
 
+## 提示词修订交接
+
+2026-09-10 / 2.1：在本地 2.0 版上增补 RCE 上层边界、六段助记顺序和 pixel-identical 确定性合成验收，保留已有四类提示模板。Prompt_Framework.md 继续维护提示写法；image-generation.md 维护执行与文件验收；AIGC_CREATIVE_RULES.md 维护实验记录要求。
+
+本次已合并本地维护源及仓库插件分发副本；云端文件和 Project 指令尚待替换核对，只同步本次变更文件。八模块旧命名与当前会话九模块的差异留待独立维护，不与提示词修订混合。
+
 ## 官方依据
 
 2026-09-10 核对：[OpenAI Image prompting](https://developers.openai.com/api/docs/guides/image-prompting)。提示原则的工作台应用维护在原 `Prompt_Framework.md`；本次不迁移模型或复制 API 参数表。
diff --git a/plugins/codex-design-visuals/.codex-plugin/plugin.json b/plugins/codex-design-visuals/.codex-plugin/plugin.json
index 3d8820a..2a6d789 100644
--- a/plugins/codex-design-visuals/.codex-plugin/plugin.json
+++ b/plugins/codex-design-visuals/.codex-plugin/plugin.json
@@ -1,6 +1,6 @@
 {
   "name": "codex-design-visuals",
-  "version": "0.6.1",
+  "version": "0.6.2",
   "description": "Codex Design 默认保留原图配色的完整花卉几何肌理静态完稿 Skill 本地测试版。",
   "author": {
     "name": "Codex Design"
diff --git a/plugins/codex-design-visuals/references/Prompt_Framework.md b/plugins/codex-design-visuals/references/Prompt_Framework.md
index e49a042..c02b14b 100644
--- a/plugins/codex-design-visuals/references/Prompt_Framework.md
+++ b/plugins/codex-design-visuals/references/Prompt_Framework.md
@@ -1,6 +1,6 @@
 # 视觉任务与生图提示编写指南
 
-版本：2.0 / 2026-09-10
+版本：2.1 / 2026-09-10
 依据：[OpenAI Image prompting](https://developers.openai.com/api/docs/guides/image-prompting)，2026-09-10 核对。
 
 ## 1. 定位与使用
@@ -14,6 +14,14 @@
 - 非生图设计任务直接说明目标、准确内容、参考用途、约束和交付标准；不强制写成生图 Prompt。
 - 动态任务在已确认静态设计上追加时长、帧率、动作、节奏、循环与镜头许可，沿用动态路由；静态图像 API 设置不适用于视频。
 
+### 1.1 RCE 上层任务契约与实际 Prompt
+
+保留 RCE（Role–Constraints–Examples，角色—约束—示例）作为上层任务契约：R 只记录影响本次判断的职责，C 记录目标、硬锁、允许变化与交付验收，E 记录参考来源、用途和边界。简单任务可在内部完成这些判断，不要求展示角色、契约或完整分析；复杂任务需要交接时再明确列出。
+
+实际提交给 GPT Image 的 Prompt 描述本轮画面，不照搬 RCE 全文、履历、仓库操作或审核流程。工具参数和后期制作清单分开记录。H1/H2/H3 应转成位置、比例和阅读关系；LOCKED 应转成具体保留项，不能因简化提示而遗漏。
+
+OpenAI 官方 Image Prompting 指南是 GPT Image 实际提示词写法的最高专业依据；RCE 负责本次任务边界与验收，两者职责不同。第 4 节四类模板继续用于实际 Prompt，不作为 RCE 固定格式。
+
 ## 2. 编写原则
 
 1. **先写结果。** 说明做什么图、给谁看、用在哪里，写清主体、画幅、构图和关键位置；不要求先输出设计师身份或完整分析。
@@ -22,11 +30,28 @@
 4. **准确文字单独标明。** 用引号包住原文，说明位置、排印和出现次数；必要时逐字澄清特殊拼写。说明不添加额外文字，生成后逐字核对可读性、数字及标点。
 5. **编辑分清改与不改。** 指定区域、对象和变化，明确身份、形状、构图、配色、光线、标签等保留项。禁止项只针对本轮实际风险，不把通用负面词库整段附加到每个提示。
 6. **参考逐张分工。** 用编号或文件名区分底图、主体、构图、风格和待合成资产，说明只采用哪些属性以及组合关系。只有参考用途的图不能替代唯一底图。
-7. **逐轮收敛。** 使用已确认输出作为下一轮输入，每轮修改一个问题或一组相关问题，重申关键保留项并检查结果。不要靠不断追加重复措辞代替失败诊断。
+7. **逐轮收敛。** 使用已确认输出作为下一轮输入，默认每轮只改一个明确变量；确需联动时记录整组范围与原因，重申关键保留项并检查结果。不要靠不断追加重复措辞代替失败诊断。
 8. **格式服务维护。** 自然段、分段指令、标签或结构化字段均可；不宣称特殊语法更有效，不强制英文或长提示。默认使用中文，保留准确专名和用户指定语言。
 
 以上是对官方原则的工作台应用。完整后期步骤、保存路径、审核记录不必塞入图像提示；需要交付时单独提供。
 
+### 2.1 六段组织顺序
+
+**Result → Subject → Composition → Visible Details → Style/Medium → Constraints**
+
+这是工作台根据官方原则整理的助记顺序，并非官方命名框架或强制语法；按任务删减，不必逐段输出。
+
+| 部分 | 需要表达的画面要求 |
+| --- | --- |
+| Result｜结果 | 图像类型、用途、画幅与交付阶段 |
+| Subject｜主体 | 对象、数量、身份、动作及入画范围 |
+| Composition｜构图 | 位置、比例、视角、裁切、空间关系、留白与文字安全区 |
+| Visible Details｜可见细节 | 形态、纹理、材质、颜色与光线 |
+| Style/Medium｜风格与媒介 | 摄影、插画、纸张丝印、三维等表现方式 |
+| Constraints｜约束 | 准确文案、保留项、修改边界、禁止项与目标输出 |
+
+第 4 节已有新建、参考、编辑和合成模板按此思路裁剪，无需增加第二套模板。
+
 ## 3. 约束、文字与设置
 
 - 沿用[参数硬锁](shared/parameter-lock.md)：用户指定的底图、主体、配色、文字、Logo、尺寸及输出要求不得被默认值覆盖。冲突按[冲突处理](shared/conflict-resolution.md)解决。
@@ -34,7 +59,7 @@
 - 用户要求图内文字时允许直接生成并逐字验收。需要标准 Logo、精确字体、可编辑排字或生产级一致性时使用官方资产及可控后期；错误文字不可作为合格结果。详细排印见[字体规则](shared/typography.md)。
 - 提示描述画面和目标画幅；`model`、`size`、`quality`、`background`、格式等实际 API 设置单列，仅在工具支持时使用。网页、Codex 工具与 API 不共用一套参数。
 - 请求透明背景不等于文件具有 alpha；写下尺寸不等于输出达到该尺寸。打开实际文件验证，不能把放大或改 PPI 当作原生细节通过。
-- 生成编辑不能保证区域像素完全不变；有此要求时，在原图上可控合成已批准修改，并对保护区作差异核验。
+- 生成编辑不能保证区域像素完全不变；要求 pixel-identical 时，必须在原图上确定性合成已批准修改，再按[生图生产路由第 3.1 节](routes/production/image-generation.md)对保护区作逐像素核验。
 
 ## 4. 四类可裁剪模板
 
diff --git a/plugins/codex-design-visuals/references/routes/production/image-generation.md b/plugins/codex-design-visuals/references/routes/production/image-generation.md
index e3db251..23b9451 100644
--- a/plugins/codex-design-visuals/references/routes/production/image-generation.md
+++ b/plugins/codex-design-visuals/references/routes/production/image-generation.md
@@ -21,7 +21,7 @@
 - 每张输入图登记：编号/文件名、底图或参考角色、允许借鉴、必须保留、不可复制。未看见文件时不凭文件名猜图。
 - 从实际图中读取主体数量、位置、方向、比例、原色、关键结构和文字区；用户指定内容设为 `LOCKED`。
 - 记录目标画幅与像素；印刷另记物理尺寸、分辨率、色彩环境和出血。比例与像素矛盾时先明确哪个是准值。
-- 编辑任务先列 `KEEP / CHANGE / LOCKED`；局部修改写明区域、对象及边界。极严格的像素保留宜在原图上用可控蒙版/图层合成验收，不能承诺生成过程绝对不影响选区外。
+- 编辑任务先列 `KEEP / CHANGE / LOCKED`；局部修改写明区域、对象及边界。要求 pixel-identical 时必须按第 3.1 节确定性合成并验收，不能承诺生成过程绝对不影响选区外。
 - 缺省值只补用户未指定项。保色、白底、无字、透明背景、镜头或风格均不能擅自替换用户明确要求。
 
 ## 3. 建立可执行提示
@@ -34,13 +34,25 @@
 
 需要图内文字时，用引号提供准确文案并明确位置、排印和出现次数，允许按用户需求直接生成，逐字复核。需要标准 Logo、精确字体、可编辑排字或严格产品标签时，保留官方资产并采用可控后期；不把生成候选当作已核验品牌终稿。
 
+### 3.1 pixel-identical 与确定性合成
+
+官方明确指出：区域必须 pixel-identical 时，应把确认后的编辑合成回原图，不能仅依靠提示词。工作台据此执行以下生产与验收要求：
+
+1. 保存原图与保护区域定义，先区分“视觉一致”与“逐像素相同”。
+2. 生成候选只提供允许修改的内容；在原图上用图层和明确蒙版合成。保护区域直接使用原图像素；羽化边缘必须位于允许修改范围内。
+3. 若承诺逐像素相同，保护区不得经历缩放、重采样、整体调色或有损重编码。保留相同像素网格、通道、位深和色彩解释；使用无损验收文件。
+4. 重开导出文件，对保护区域逐通道比较像素值（含 alpha）。差异像素数与最大通道差均为 0，才记录该区域 pixel-identical 通过；视觉预览、OCR 或文件大小相同不能替代此项检查。
+5. 另需 JPEG、改尺寸或转换色彩时，单独验收其用途，不沿用无损母版的像素一致结论。
+
+这些步骤是工作台的后期验收约定；生成工具返回成功不代表已完成确定性合成。
+
 ## 4. 生成、选择与返修
 
 1. 上传或展示本轮真正使用的输入图；本地图片先实际打开检查。
 2. 按用户要求的数量生成；未指定时先做一个明确方向，避免无目的批量消耗。
 3. 对照输入与硬锁检查候选，而不只凭“好看”选择；标出问题属于主体、构图、字体、颜色、肌理还是生产规格。
-4. 沿用已确认版本，每次修改一个明确问题或同一组相关问题；重申保留项，禁止悄悄换底图。
-5. 修改保护区域附近内容后，对照原图检查边缘、位置、文字安全区和品牌资产；有像素保留要求时在相同尺寸、色彩条件下核对区域差异。
+4. 将上一轮已确认输出作为下一轮输入，默认每轮只改一个明确变量；确需联动时记录整组范围与原因。重申保留项，先比较结果再补充指令，禁止悄悄换底图。
+5. 记录输入/输出版本、实际 Prompt、参数、本轮变量和通过/失败原因；对照原图检查边缘、位置、文字安全区和品牌资产。pixel-identical 按第 3.1 节验收。
 6. 发生持续主体漂移或局部无法受控时，回到已确认母版，缩小编辑范围或转入确定性的后期制作；不通过反复整图重生成掩盖失败。
 
 ## 5. 验收与 Adobe 交接
