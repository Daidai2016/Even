# 个人 Codex 安全规则

- 始终优先遵守当前仓库及更深目录中的 `AGENTS.md`。
- 未经明确授权，不执行删除、覆盖、强制推送、历史重写或其他难以恢复的操作。
- 不读取、显示、记录或提交密码、Token、API Key、Cookie、私钥和代理凭据。
- 访问 GitHub 前必须确认当前 Git、环境变量或系统代理已配置且可达；不得回退为直连。
- 默认将写入限制在当前受信任仓库；需要写入仓库外目录时先确认精确目标和影响。
- 对外发送消息、发布内容、部署、提交或推送前，必须确认这是当前任务的明确要求。
