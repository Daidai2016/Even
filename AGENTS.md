# Codex Design 仓库工作规则

## 1. 文档定位与优先级

本文档是仓库级行为规范，适用于整个仓库。

规则优先级从高到低为：

1. 用户在当前任务中的明确要求。
2. 更深层目录中的 `AGENTS.md`。
3. 本文档。
4. 根目录 `CODING_RULES.md` 及各类说明文档。

文档职责：

- `README.md`：项目概览、入口和工作台说明。
- `AGENTS.md`：仓库级操作、安全和交付规则。
- `CODING_RULES.md`：代码、自动化工具和创意交付物的通用开发规范。
- `docs/AIGC_CREATIVE_RULES.md`：AIGC 图像生成、创意训练和 skill 建设的专项规范。
- 子目录 `AGENTS.md`：该软件或目录的局部强制规则。
- `scripts/*/docs/README.md`：软件定位、能力和工作流指南，不重复强制规则。
- `tools/docs/ENVIRONMENT.md`：当前测试环境事实，不定义通用开发规则。

## 2. 项目定位

Codex Design 是 AI 辅助创意工作流开发与生产平台。项目有两条同级主线：

1. **AIGC 图像生成能力建设**：建立审美、版式、字体和形式创意能力，沉淀为可复用的提示词、参考资产、评价标准、工作流和 skills。
2. **设计便捷工具建设**：开发脚本、动作、模板、生成器和多工具组合技巧，减少重复操作，提高设计、数据、汇报和交付效率。

Adobe、PowerPoint、Excel、Word、PDF 和其他创意工具都是两条主线的可用平台，不单独构成项目定位。

### 2.1 主线边界

- AIGC 主线定义“生成什么、什么算好、如何评价”。
- 工具主线定义“如何稳定、快速、可重复地完成”。
- AIGC 标准可成为工具的输入；工具不得擅自改变 AIGC 的审美、版式、字体或创意标准。
- 同一任务可横跨两条主线，但必须分别写明创意目标、工具实现和验收结果。

工作优先级：

1. 创意与生产结果正确。
2. 无人值守运行稳定。
3. 符合实际制作流程。
4. 文件交付可靠。
5. 符合操作者习惯。
6. 代码结构清晰。
7. 模块化与复用。

不得为了代码简洁、通用化或架构优化，改变已确认的制作流程、文件规则、输出方式、报告格式或交付逻辑。

文件命名、报告内容、输出说明和工作流应面向设计师、制作人员和供应商，避免直接暴露无意义的内部技术信息。

## 3. 任务工作流

处理修改任务时，按以下顺序执行：

1. 理解需求并阅读适用规则。
2. 检查现有结构、逻辑和 Git 状态。
3. 说明修改目的、涉及文件、方案和潜在影响。
4. 采用能完成目标的最小安全修改。
5. 按风险程度验证结果。
6. 报告变更、验证结果、未验证风险和 Git 状态。

修改已有生产脚本时，未经用户明确同意，不得大规模重构、重写脚本、拆分大量模块或改变执行流程。

## 4. 仓库与文件管理

- 当前 Git 仓库是默认项目工作区。
- 正式脚本、文档和交付文件必须保存在仓库内，除非用户明确指定其他位置。
- 文档、配置和脚本中使用相对于仓库根目录的路径；不得硬编码某台电脑的仓库绝对路径。
- Adobe 生产脚本放在 `scripts/` 下对应的软件目录。其他交付物生成代码按已有项目结构放在对应工作流或工具目录，不得为方便而全部塞入 Adobe 目录。
- 仓库管理、环境检查和快捷方式生成等 PowerShell 工具放在 `tools/`。
- 通用文档和 Word 类交付物放在 `docs/` 或任务已建立的专项目录；演示文稿放在 `presentation/`；软件专项文档放在对应软件目录的 `docs/`。
- 可复用提示词放在 `prompts/`，经验证的流程放在 `workflows/`，参考图像、色板、字体样例和其他合法资源放在 `assets/`。
- 未验证的 AIGC 方向测试放在 `experiments/`，可清理的临时文件放在 `work/`；两者都不得冒充正式成果。
- 可复用 skill 按 `docs/AIGC_CREATIVE_RULES.md` 统一管理。
- 需要进入版本管理的大型设计、演示和思维导图文件（如 `.ai`、`.psd`、`.psb`、`.indd`、`.tif`、`.pptx`、`.xmind`）必须按 `.gitattributes` 中的 Git LFS 策略管理。
- 桌面快捷方式和测试临时产物不得加入 Git。

## 5. 稳定版本与删除保护

- 不主动删除已有脚本、用户文件或生产成果。
- 修改已有生产脚本时，默认保留原始稳定版本，并创建递增版本；只有用户明确要求时才可覆盖、重命名或删除原版本。
- 执行删除、批量移动或覆盖前，必须确认精确目标和影响范围。

## 6. 报告与交付

- 运行报告面向操作者，记录执行结果、异常和耗时。
- 交付说明面向客户和制作供应商，记录文件规格和制作参数。
- 两类文档不得混用，不得向客户暴露内部调试信息。
- 任务完成前检查文件位置、稳定版本、临时文件、无关修改和 Git 状态。

## 7. Git 操作权限

可在不改变仓库状态的前提下执行 `git status`、`git diff`、`git diff --stat` 和其他本地只读检查。

只有用户明确要求时才可执行 `git commit` 或 `git push`。不得擅自执行 `git reset --hard`、删除已有提交、强制推送或覆盖远程分支。

如果用户已说明手动完成了提交或发布，不得重复执行。

## 8. GitHub 代理强制规则

本节适用于所有访问 GitHub 的操作，包括 `git fetch`、`git pull`、`git push`、`gh` 命令以及由脚本或桌面快捷方式发起的发布与同步。

- 只允许使用当前电脑正在使用的代理服务器访问 GitHub，禁止直连。
- 执行 GitHub 网络操作前，必须读取当前生效的 Git 或系统代理配置，并确认代理服务器可用。
- 未检测到代理或代理不可用时，必须停止操作并报告原因，不得回退到直连。
- 不得删除、覆盖、绕过、禁用或自动改写现有代理配置。
- 不得硬编码某个代理地址或端口；必须每次使用当前环境的实际生效值。
- 不得通过修改 Git TLS 后端、忽略证书验证或其他方式绕过代理和安全配置。
- `git status`、`git diff` 等不访问远程的本地只读检查不受本节限制。

如本节与其他 GitHub 操作说明冲突，以“仅使用当前有效代理，禁止直连”为准。

## 9. 用户操作习惯

对打开项目、Git 状态检查、GitHub 发布与同步等重复操作，优先维护图形化快捷方式。除故障排查或首次配置外，不应优先要求用户手动输入终端命令。

## 10. 文件编码

- PowerShell (`.ps1`)：UTF-8 with BOM，并保持 Windows PowerShell 5.1 兼容。
- Adobe ExtendScript (`.jsx`)：UTF-8，具体语法要求见 `CODING_RULES.md`。
- Markdown、JSON 和 SVG：UTF-8。
- PowerPoint、Excel、Word 和 PDF 等二进制交付物不适用文本编码规则；必须使用对应格式的标准生成、打开和验证流程。
