# 创意工作流通用开发规范

版本：V4.0

## 1. 文档定位

本文档定义 Codex Design 项目中代码、自动化工具和创意交付物的通用开发规范。

适用范围包括但不限于：

- Adobe Creative Cloud 自动化脚本。
- PowerShell、Python、JavaScript 等工具脚本。
- PowerPoint 汇报和演示文稿生成。
- Excel、CSV 及其他数据表格处理。
- Word 文档、分析报告和说明书生成。
- PDF、图像、图标和其他创意交付物的生成与验证。

仓库操作、Git 权限、GitHub 代理、文件存放和交付安全规则由根目录 `AGENTS.md` 统一定义。特定软件或目录的局部强制规则由对应目录中的 `AGENTS.md` 定义。

AIGC 的审美、版式、字体、形式创意和 skill 内容标准由 `docs/AIGC_CREATIVE_RULES.md` 定义；本文档只规定其实现、文件和验证底线。

## 2. 成果优先原则

开发必须服务于真实创意与生产流程。优先级为：

1. 结果准确，内容与数据可信。
2. 交付物可打开、可阅读、可编辑、可复用。
3. 工作流稳定，批量和无人值守运行可靠。
4. 符合使用者的阅读、演示、分析和制作习惯。
5. 实现清晰、可维护，必要时可复现。

不得为了代码简洁、通用化或视觉炫技，改变已确认的业务逻辑、生产参数、数据口径、文件结构、输出格式或交付标准。

## 3. 修改现有工作流

修改前必须明确：

1. 当前成果或工具的用途和使用对象。
2. 输入、处理逻辑、输出和外部依赖。
3. 修改目标、涉及区域和可能影响。
4. 验证方法、失败处理和回退方案。

默认采用能完成目标的最小安全修改。未经确认，不得重写完整工作流、大规模重构、替换数据口径、改变版式系统或改变交付结果。

## 4. 输入、数据与来源

- 明确区分用户原始输入、中间计算、外部资料和最终结论。
- 数据字段必须保留单位、时间范围、口径、来源和缺失值含义。
- 不得把模拟数据、占位内容或未验证推断伪装成真实结果。
- 引用外部数据、图片、图表或文本时，保留可追溯的来源说明，并遵守版权和授权要求。
- 对关键结论和汇总数字进行反向核对，不只依赖最终显示值。

## 5. 结构、依赖与可复现性

- 优先复用仓库现有模板、资产、样式和工具链。
- 只引入完成任务必需的依赖，不为一次性需求增加难以维护的复杂环境。
- 中间产物与最终交付物分开；可重建的临时文件不得与正式成果混在一起。
- 必要时保留生成脚本、数据映射、公式或模板，使成果可更新和复现。

## 6. 代码与运行环境

### 6.1 PowerShell

- 兼容 Windows PowerShell 5.1，除非任务明确指定更高版本。
- 路径使用 `$PSScriptRoot`、`Join-Path` 和 `-LiteralPath` 等安全方式处理。
- 检查外部命令退出码，并为不可恢复错误返回非零退出码。

### 6.2 Adobe ExtendScript / JSX

默认按 JavaScript ES3 能力编写。禁止主动使用 `let`、`const`、箭头函数、`class`、`import`、`export`、模板字符串及依赖现代浏览器或 Node.js 的 API。

推荐使用 `var`、普通 `function`、明确的 `try/catch` 和 Adobe 官方对象模型。

```javascript
var doc = app.activeDocument;

function processFile() {
    // 生产处理逻辑
}
```

Adobe 生产参数、文件命名和专项验证要求见对应软件目录的 `AGENTS.md`。

### 6.3 其他语言与生成工具

- 使用仓库已配置的运行时和库，不随意替换工具链。
- 生成代码与交付物分开保存，不在交付文件中夹带调试数据。
- 对非显而易见的业务逻辑添加注释，优先说明“为什么”而不是重复代码动作。

## 7. 设计便捷工具专项规范

- 一个工具聚焦一个明确流程，写明使用对象、触发方式、输入、输出、前置条件和非目标。
- 脚本负责可计算、可批处理的逻辑；动作负责应用程序内稳定的固定操作；模板负责结构和样式；工作流文档负责多工具的交接顺序。
- 不在多个工具中重复实现同一核心逻辑。必须重用时，确定唯一真实来源和清晰调用关系。
- 多工具组合必须明确每一步的输入格式、输出格式、文件位置、责任工具和失败后续处理。
- 优先使用应用程序原生能力和仓库现有工具，只在能明确降低重复操作或错误率时才增加新环节。
- 可能覆盖、批量修改或发布文件的工具，必须先检查状态、给出明确预览或确认，并在失败时保护源文件。
- 高频重复操作应提供符合用户习惯的入口，例如快捷方式、菜单、面板或批处理命令，而不是让用户重复组装技术参数。

## 8. 交付物专项规范

### 8.1 AIGC 图像与 Skills

- 保留可复现的提示词、参考资产、必要参数和生成版本信息。
- 实验结果与已验证成果分开；只有通过多样例复测的方法才可沉淀为 skill。
- skill 的内容、结构和评价方法统一遵守 `docs/AIGC_CREATIVE_RULES.md`。
- 人工视觉审核是正式交付前的必要环节，不得仅以“生成成功”作为验收。

### 8.2 PowerPoint 与演示文稿

- 先明确受众、演示场景、页面比例、时长和核心结论。
- 每页只承担一个主要信息，建立稳定的字号、间距、色彩和版式层级。
- 文字、图表和关键图形应保持可编辑；不将整页内容无必要地扁平化成一张图。
- 数据图表必须与源数据一致，必要时标注单位、时间范围和来源。
- 交付前渲染检查全部页面，确认无溢出、遮挡、字体替换、低清图像或版式偏移。

### 8.3 Excel 与数据表格

- 区分原始数据、清洗结果、计算区域、汇总结论和可视化页。
- 字段名、单位、日期、类型和缺失值处理必须一致。
- 优先使用可审计的公式或查询逻辑，不用手工粘贴的静态数字替代本应自动计算的结果。
- 设置合理的数字格式、列宽、冻结区域、筛选和条件格式，但不用样式掩盖错误。
- 交付前重新计算并检查公式错误、断开引用、总计差异和异常值。

### 8.4 Word 与长文档

- 使用标题样式、正文样式、列表、题注和页面分节建立结构，不用手动空格和空行模拟排版。
- 目录、页码、交叉引用、图表编号和页眉页脚应可更新。
- 表格、图片和图注应在页面范围内，关键图像具有必要的说明或替代文本。
- 交付前渲染检查分页、孤行、空白页、字体、表格溢出和图片清晰度。

### 8.5 PDF 与导出文件

- 可编辑源文件是主要修改对象；PDF 通常是审阅或交付版本。
- 导出后检查页数、页尺寸、字体、链接、图像、裁切、色彩和透明效果。
- 不得因为导出成功就假定视觉结果正确；必须渲染或打开复核。

### 8.6 图像与视觉资产

- 区分源文件、可编辑中间文件和最终导出文件。
- 明确像素尺寸、物理尺寸、色彩空间、透明度和输出格式。
- 不得未经确认改变图像规模、色彩意图、品牌色或关键视觉元素。

## 9. 无人值守与异常处理

- 批处理不得依赖临时点击确认、手动选择文件或人工修复单个异常。
- 单个输入失败时，默认记录异常并继续处理其他输入；如错误可能造成批量错误输出或破坏源文件，应安全停止。
- 文件打开、读取、转换、保存、导出和关闭必须包含异常处理。
- 失败后不得遗留伪装成正式成果的半成品。

## 10. 命名、报告与日志

- 文件名应能说明用途、项目、阶段或版本，不使用 `new`、`最终版`、`测试1` 等含混名称。
- 使用者可见的标题、列名、图例、状态和错误信息必须使用清晰的业务语言。
- 报告与交付说明的阅读对象和信息边界统一遵守 `AGENTS.md`。
- 日志优先使用汇总信息和必要异常记录，不生成大量无意义的单文件日志。

## 11. 验证与交付门槛

交付前必须根据成果类型完成结构验证、内容验证和视觉验证：

- 代码与脚本：语法、依赖、路径、退出码、异常流程和代表性输入。
- 演示文稿：页面结构、可编辑性、字体、图表数据和逐页渲染。
- 表格：数据类型、公式、总计、错误值、单位、来源和重新计算。
- 文档：标题层级、分页、表格、图片、字体、目录和渲染结果。
- PDF 和图像：页面或像素尺寸、色彩、裁切、清晰度、透明度和实际打开/渲染结果。

无法在当前环境完成的验证，必须在交付说明中列明，不得将“已生成”等同于“已验证”。
