# Task Route｜poster_design

用于从内容与传播目标建立静态海报。先锁定准确文案、Logo、品牌色、画幅和输出，再确定 H1/H2/H3、网格、安全区、字体角色与主体关系。

- 标题可为 H1，主体也可为 H1；由传播目标决定。
- 准确文字不交给生图模型；预留排版区并在可控工具中完成。
- 多画幅适配要重排层级和安全区，不做机械缩放。

- **PASS**：内容完整、阅读顺序清楚、静态缩略图与目标尺寸都成立。
- **FAIL**：遗漏信息、假文字、多个 H1、锁定画幅漂移或装饰抢焦。
