# Task Route｜motion_poster

用于把已确认的静态视觉系统延展为短时动态海报。先完成静态母版审计，再进入 Motion Router。

必需参数：画幅、时长、帧率、输出格式、H1/H2/H3、M1/M2/M3、首尾帧、节奏、循环、转场、镜头许可、音频策略。用户明确值均为 `LOCKED`。

- **PASS**：静态识别与文字准确性不因运动受损；首尾帧可用；动态层级服务视觉层级。
- **WARNING**：仅有扁平素材，遮挡、逐瓣展开或连续形变受限。
- **FAIL**：把元素运动当镜头运动；背景漂移；形态跳变；循环接点闪烁；擅自改时长或帧率。
