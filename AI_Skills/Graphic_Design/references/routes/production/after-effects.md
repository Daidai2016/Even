# Production Route｜after_effects

适用于分层时间轴、关键帧、缓动、遮罩、形状图层、循环与视频输出。导入前确认图层职责和锚点；文字、Logo 与主体尽量保持独立。

检查合成尺寸、时长、帧率、色彩、运动模糊、首尾帧、音频、编码和平台安全区。

- **PASS**：M1/M2/M3 可在时间轴定位，关键帧与输出参数匹配 `LOCKED`。
- **FAIL**：预合成隐藏不可控漂移；背景或镜头无授权运动；只输出视频而无约定工程或复核记录。
