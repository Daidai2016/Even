# Production Route｜image_generation

生图用于建立或转译视觉素材，不默认承担准确文字、Logo、精确路径和最终像素尺寸。

输入 Prompt 必须包含 `LOCKED`、正向结构、材质参数和负面约束。输出后检查主体数量、身份、文字伪影、边缘、色彩漂移、水印和目标画幅。

- **PASS**：候选图可作为后期素材，且未违反硬锁。
- **WARNING**：像素尺寸、字体或图层仍需后期。
- **FAIL**：把候选图直接宣称为可编辑终稿，或让模型生成必须逐字准确的信息。
