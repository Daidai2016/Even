# Motion Router｜Timing

记录总时长、帧率、节奏段、启动顺序、停留时间和缓动。用户指定值为 `LOCKED`。

建议把时间组织为进入、发展、稳定/退出三个阶段；短循环可合并，但仍需识别节拍和停留。关键文字至少保留可阅读时窗。

- **PASS**：关键事件能换算到具体秒数或帧；节奏服务阅读和 M1/M2/M3。
- **WARNING**：平台压缩或低帧率可能影响细纹理和缓动。
- **FAIL**：擅自改时长/帧率；动作同时启动；尾帧没有停留导致无法读取。
