# This file is safe to commit. Replace <PORT> with the HTTP proxy port
# detected on the current computer, or run tools/create_codex_env.ps1.
HTTP_PROXY=http://127.0.0.1:<PORT>
HTTPS_PROXY=http://127.0.0.1:<PORT>
