# Task Route｜material_redraw

用于把既有主体转换为新的造型、色彩或材质语言，同时保留用户指定的身份与结构。

1. 锁定主体数量、身份、姿态、构图、画幅、背景、颜色和文字策略。
2. 区分“只换风格”“允许抽象”“允许换色”“允许重构构图”。未明确允许的变化一律不执行。
3. 列出原图识别点与目标视觉语法，逐项建立映射。
4. 输出主 Prompt、负面约束、后期拆层和识别度审计。

- **PASS**：目标风格覆盖完整主体，关键识别点与 `LOCKED` 均保留。
- **FAIL**：只处理局部导致风格混杂；借风格转绘改写主体、颜色、背景或文字。
